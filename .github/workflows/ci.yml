name: CI

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  build-release-linux:
    name: Build Release Binary (Linux)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install LLVM
        run: |
          wget https://apt.llvm.org/llvm.sh
          chmod +x llvm.sh
          sudo ./llvm.sh 21 all
          sudo apt-get install -y llvm-21-dev libpolly-21-dev
          echo "LLVM_SYS_211_PREFIX=/usr/lib/llvm-21" >> $GITHUB_ENV

      - name: Build compiler (release)
        run: cargo build --release

      - name: Upload compiler artifact
        uses: actions/upload-artifact@v4
        with:
          name: apex-compiler-linux-release
          path: target/release/apex-compiler

  rust:
    name: Rust Checks
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install LLVM
        run: |
          wget https://apt.llvm.org/llvm.sh
          chmod +x llvm.sh
          sudo ./llvm.sh 21 all
          sudo apt-get install -y llvm-21-dev libpolly-21-dev
          echo "LLVM_SYS_211_PREFIX=/usr/lib/llvm-21" >> $GITHUB_ENV

      - name: Cache cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}

      - name: Cargo check (all targets/features)
        run: cargo check --locked --all-targets --all-features --verbose

      - name: Run tests
        run: cargo test --locked --verbose

      - name: Run release tests
        run: cargo test --locked --release --all-targets --verbose

      - name: Check formatting
        run: cargo fmt -- --check

      - name: Run clippy
        run: cargo clippy --locked --all-targets --all-features -- -D warnings

  cli-smoke:
    name: CLI Smoke
    runs-on: ubuntu-latest
    needs: build-release-linux

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install LLVM
        run: |
          wget https://apt.llvm.org/llvm.sh
          chmod +x llvm.sh
          sudo ./llvm.sh 21 all
          sudo apt-get install -y llvm-21-dev libpolly-21-dev
          echo "LLVM_SYS_211_PREFIX=/usr/lib/llvm-21" >> $GITHUB_ENV

      - name: Download compiler artifact
        uses: actions/download-artifact@v4
        with:
          name: apex-compiler-linux-release
          path: target/release

      - name: Ensure compiler is executable
        run: chmod +x target/release/apex-compiler

      - name: Run CLI smoke coverage
        run: bash scripts/ci_cli_smoke.sh
        env:
          CI_SKIP_COMPILER_BUILD: "1"
          APEX_COMPILER_PATH: ${{ github.workspace }}/target/release/apex-compiler

  check-examples:
    name: Check Examples
    runs-on: ubuntu-latest
    needs: build-release-linux
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install LLVM
        run: |
          wget https://apt.llvm.org/llvm.sh
          chmod +x llvm.sh
          sudo ./llvm.sh 21 all
          sudo apt-get install -y llvm-21-dev libpolly-21-dev
          echo "LLVM_SYS_211_PREFIX=/usr/lib/llvm-21" >> $GITHUB_ENV

      - name: Download compiler artifact
        uses: actions/download-artifact@v4
        with:
          name: apex-compiler-linux-release
          path: target/release

      - name: Ensure compiler is executable
        run: chmod +x target/release/apex-compiler

      - name: Check and run all examples
        run: |
          COMPILER=./target/release/apex-compiler

          # Single-file examples
          for file in examples/*.apex; do
            echo "Checking $file..."
            "$COMPILER" check "$file"
            echo "Running $file..."
            "$COMPILER" run "$file"
          done
          
          # Multi-file projects
          for project in examples/multi_file_project examples/multi_file_depth_project examples/test_no_import; do
            if [ -d "$project" ]; then
              echo "Testing project $project..."
              cd "$project"
              ../../target/release/apex-compiler run
              cd ../..
            fi
          done

  web:
    name: Web Checks
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: web

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: web/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Type check
        run: npm run check

      - name: Run tests
        run: npm test

      - name: Build web app
        run: npm run build
